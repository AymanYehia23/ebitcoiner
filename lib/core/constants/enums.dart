enum Currency {
  btc,
  eth,
  rvn,
  ltct,
  non,
}

enum Period{
  long,
  short
}

enum AsicStatus{
  online,
  offline,
  onDemand,
}

enum TransactionStatus{
  processing,
  sent,
  failed,
}

enum LocalAuth{
  authenticated,
  notAuthenticate,
  securityRequired,
}
